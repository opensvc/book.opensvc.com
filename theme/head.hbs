<!-- Version picker -->
<script>
// One book is built per agent release and deployed under its own path, so the
// reader has to be told which one they are reading and given a way to move
// between them. The list of releases lives at the site root, outside any of
// them, because a book cannot know about the releases that come after it.
(function () {
  var current = window.location.pathname.split("/")[1] || "";

  function build(versions) {
    var bar = document.getElementById("menu-bar");
    if (!bar || !versions || !versions.length) {
      return;
    }
    var buttons = bar.querySelector(".right-buttons") || bar;
    var select = document.createElement("select");
    select.className = "version-picker";
    select.setAttribute("aria-label", "documentation version");
    versions.forEach(function (version) {
      var option = document.createElement("option");
      // A version is {"name": "v3.1", "path": "/v3.1/"} : the name is what
      // the reader picks from, the path is where that book is deployed.
      option.value = version.path;
      option.textContent = version.name;
      if (version.path.split("/")[1] === current) {
        option.selected = true;
      }
      select.appendChild(option);
    });
    select.addEventListener("change", function () {
      // The same page of the chosen release, and its home page when that
      // release has no such page: a keyword that was added or removed is
      // exactly what a reader switches versions to check.
      var rest = window.location.pathname.split("/").slice(2).join("/");
      window.location.href = this.value + rest;
    });
    buttons.appendChild(select);
  }

  window.addEventListener("DOMContentLoaded", function () {
    fetch("/versions.json", { cache: "no-cache" })
      .then(function (r) { return r.ok ? r.json() : null; })
      .then(build)
      .catch(function () { /* no list, no picker */ });
  });
})();
</script>
<!-- End version picker -->

<!-- Matomo -->
<script>
  var _paq = window._paq = window._paq || [];
  /* tracker methods like "setCustomDimension" should be called before "trackPageView" */
  _paq.push(['trackPageView']);
  _paq.push(['enableLinkTracking']);
  (function() {
    var u="//matomo.opensvc.com/";
    _paq.push(['setTrackerUrl', u+'matomo.php']);
    _paq.push(['setSiteId', '2']);
    var d=document, g=d.createElement('script'), s=d.getElementsByTagName('script')[0];
    g.async=true; g.src=u+'matomo.js'; s.parentNode.insertBefore(g,s);
  })();
</script>
<!-- End Matomo Code -->
