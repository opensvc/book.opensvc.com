# Section `DEFAULT`

**Supported keywords:**

- comment
- id

## Keyword `comment`

	required:    false
	scopable:    false

**Description:**

A free form text describing the role of the object, of the node, or of the
section it is set in.

The keyword is accepted in any section, so the DEFAULT section can document a
configuration as a whole, and a resource, pool, heartbeat, array or network
section can document itself.

The agent does not interpret the value.


## Keyword `id`

	required:    false
	scopable:    false

**Default:**

An autogenerated random uuid

**Description:**

A RFC 4122 random uuid identifying the node, generated by the agent on first
use and stored in the node configuration.

It is what the `{id}` reference expands to in the node configuration.


